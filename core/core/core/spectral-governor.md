# QEI-AI Spectral Governor

The Spectral Governor defines adaptive spectral-routing rules that maintain deterministic behavior across shifting runtime conditions.  
It enforces invariant boundaries, field quantization rules, and signed-state transitions.
